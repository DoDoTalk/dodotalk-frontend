<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".DoDoTalkApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.App.Starting">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- autoVerify : 서버에 있는 assetlinks.json을 이용해 scheme과 host domain 소유 여부를 시스템이 검증하도록 설정하는 변수 -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <!-- https를 사용하는 경우 해당 도메인의 소유자임을 증명하기 위해 서버에 인증 파일을 업로드 하는 등의 작업이 필요하다. -->
                <data android:scheme="https" />
                <data android:host="dodotalk.shop" />

                <data android:pathPrefix="/api/auth/verify" />
                <data android:pathPrefix="/api/auth/reset-password" />
            </intent-filter>

            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <!-- dodotalk://dodotalk.shop/api/auth/verify -->
                <data android:scheme="dodotalk" />
                <data android:host="dodotalk.shop" />

                <data android:pathPrefix="/api/auth/verify" />
                <data android:pathPrefix="/api/auth/reset-password" />
            </intent-filter>
        </activity>
    </application>

</manifest>
